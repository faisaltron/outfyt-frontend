import 'package:flutter/material.dart';

class AppTheme {
  // Primary color used throughout the application.
  static const Color primaryColor = Color(0xFFFF69B4); // Pinkish color

  // Consider expanding this class with more theme data, such as:
  // - Text themes (headline1, bodyText1, etc.)
  // - Button themes (ElevatedButtonThemeData, TextButtonThemeData)
  // - InputDecorationTheme for text fields
  // -ColorScheme for a more detailed color palette
  // This helps in maintaining a consistent UI and makes future theme changes easier.
}
